use card from "./card.wx";
use { card, title } from "./card.wx";

/* Root Html Template */
tmpl @base:
  <-card />
  <-card />;

/* Css Stylesheet */
styl card {
  body {
    display: flex;
  }
  .card {
    width: 200px;
    height: 250px;

    background-color: #aaa;
    margin-right: 16px;

    display: flex;
    flex-direction: column;
    align-items: center;
  }
}